[package]
name = "no_loss_auction"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = { version = "21.7.7" }

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true
